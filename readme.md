Create a virtual environment, activate it, and install this project’s dependencies from requirements.txt:
```bash
$ virtualenv env
$ source env/bin/activate
(env) $ pip3 install -r requirements.txt
```

Seed the Database:
```bash
(env) $ python3 seed_database.py
```